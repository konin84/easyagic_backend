# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# Uploaded media (local dev only — Cloudinary in production)
media/

# Filled-in payment config (real bank details) — only the .example is tracked
payment_config.json
